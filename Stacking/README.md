Performs model stacking on other models in this repo.

	LOGIT_ON_STACKED: preforms logistic regression during model stacking.  Simply run it.
	
	SVC_ON_STACKED: preforms SVC during model stacking.  Simply run it.